{
  "name": "happylife-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor shell for Happy Life web app",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "typescript": "^6.0.3"
  }
}

# Happy Life — дорожная карта и шаги (TODO)

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` готово — можно обновлять вручную.

## Фаза 0: продукт и право

- [ ] Зафиксировать **границы MVP**: что входит в первый релиз, что откладывается (см. `DEVELOPMENT_CONTEXT.md`).
- [ ] Описать **модель доверия**: верификация адреса (код от УК, скан документа, ручной апрув — выбрать один сценарий).
- [ ] Проверить **152-ФЗ / персональные данные**: политика, согласие, сроки хранения, доступ модератора к «анонимным» авторам.
- [ ] Решить юридический статус **голосований** (опрос vs решение общего собрания — не смешивать без юридической оценки).

## Фаза 1: дизайн и UX

- [ ] Собрать **CJM** для жителя и для председателя (ключевые экраны и боли).
- [ ] Прототип **нижней навигации** (согласовать 4–5 табов vs «6 табов» из DOC — избежать дублирования «голосования / чат»).
- [ ] Мобильный **first** wireframe: главная, заявки, голосования, сообщество (доска + чат).
- [ ] Редакционный гайд: **типографика, пустые состояния**, язык уведомлений.
- [ ] **Иконка Android-приложения** Happy Life: макет 1024×1024, замена `mipmap-*` / adaptive icon в `mobile/android`, пересборка APK и публикация на сайте (см. `mobile/README.md`).

## Фаза 2: архитектура (без кода в первой итерации — только решения)

- [ ] Выбрать стек (например: веб-фреймворк, БД, хранилище файлов, очередь уведомлений).
- [ ] Модель данных: пользователь, участок/дом, роль, заявка, голосование, пост, тема чата, анонимное обращение.
- [ ] Стратегия **фото и геометок**: приватность соседей, ретенция.
- [ ] Очередь уведомлений: email → затем push (отдельная задача).

## Фаза 3: MVP разработка

- [x] **Multi-tenant:** `Community`, `communityId`, суперадмин `/platform/communities`.
- [x] Онбординг поселка `/register-community` + **модерация** (`approvedAt`) суперадмином.
- [x] Регистрация жителя **только по invite-коду**; смена кода председателем в профиле.
- [x] Суперадмин: блок/удаление поселка и **управление председателями** (код подтверждения).
- [x] Личные сообщения `/messages` (1-на-1).
- [x] Auth + профиль + **привязка к адресу** + роли.
- [x] Параметр жителя при регистрации: **собственник / арендатор** (`tenancyType`).
- [x] Аудитория контента: `audience` для **голосований** и **тем форума** (все / только собственники / только арендаторы).
- [x] Главная: новости, активные голосования и заявки (кликабельные секции); карточка начислений за дом → `/payments` (без сетки быстрых кнопок).
- [x] Модуль **заявок** (CRUD, статусы, список+модалка, архив «Решено»).
- [~] Модуль **голосований** (участие только верифицированных, подсчёт, архив).
- [x] **Доска объявлений** по категориям.
- [x] **Упрощённый форум/темы** (MVP без полного клона Threads).
- [x] **Конфиденциальные обращения** + экран модератора.
- [x] **Мои платежи** (бывш. счётчики): абонплата + электроэнергия по **дому** (`HouseholdBilling`), не по пользователю. Показания счётчиков жителем убраны; `/meters` → `/payments`.
- [x] **Биллинг по месяцам:** `periodYear`/`periodMonth`, переключатель месяца для головы, история для жителя, отметка «Оплачено» (`paidAt`).
- [x] **Справочник адресов** `CommunityAddress` + выбор при регистрации (`/chair/addresses`).
- [x] **Навигация UX:** дубли убраны из «Ще» (в т.ч. «Управление» для головы); мешканці в хабе «Сообщество»; тарифы — с `/payments`.
- [x] **Список+модалка:** заявки (`RequestsPanel`), жители у головы (`ChairUsersPanel`).

### Ближайшие шаги (следующая итерация)

- [ ] Платежи: копирование начислений с прошлого месяца; фильтр «только неоплаченные»; привязка `HouseholdBilling` к `communityAddressId`.
- [ ] Показать/выбирать `audience` в UI в карточках голоса/темы (бейдж «только собственники» и т.п.).
- [ ] Фильтрация **доски объявлений** по аудитории (если нужно) + миграция данных.
- [x] Сценарии модерации: удаление контента (форум/доска/голосования) через панель.
- [~] Настройки посёлка: invite-код (есть), название/locale — частично; нет UI смены названия головой.
- [x] Уточнить политику «конфиденциальных обращений»: **автора видит только модератор**, председатель — нет (даже в панели).
- [x] Разделить роли: модератор = **пользователи + конфиденциальные обращения**, без публикаций/финансов/участия как житель.
- [x] Редактирование профиля: житель может менять **свои** данные (имя/адрес/телефон/статус проживания), а модератор/председатель — данные **всех** пользователей.
- [x] Информационные страницы: тарифы и меморандум; согласие с меморандумом при регистрации.
- [x] Каталог жителей: `/{locale}/residents` (вход с `/community`).
- [x] Редактирование: авторы могут редактировать свои объявления и темы форума.
- [x] Перейти с локального SQLite на Postgres (Neon) — production деплой работает на Vercel + Neon.
- [x] Чистка одноразовых скриптов и мёртвого кода (миграция SQLite→PG, codemod-ы, сгенерированный sqlite-клиент, неиспользуемые экспорты в `enums.ts`).
- [x] Миграции Prisma: `prisma migrate deploy` на Vercel build (`vercel-build`).
- [~] Расширить покрытие тестами (Vitest + Playwright e2e — база есть).
- [ ] Вынести загрузки из `public/uploads` в S3-совместимое хранилище (на Vercel `public/uploads/` эфемерный).
- [ ] Next.js 16: переименовать `web/src/middleware.ts` → `web/src/proxy.ts` (deprecation warning в `next build`), проверить совместимость с `next-intl`.
- [x] Capacitor Android: проект в `mobile/android`, debug APK на проде (`/api/download/apk`).
- [ ] Кастомная **иконка и splash** приложения + release-сборка (подпись keystore) перед массовой раздачей жителям.

## Фаза 4: качество и запуск

- [~] Тесты критических сценариев (unit + e2e registration; расширить platform/multi-tenant).
- [~] Пилот: **«Щасливе Життя»** на проде; подключать 2–3 КГ.
- [ ] Итерация по UX и приоритизация бэклога.

## Фаза 5: усиление (после пилота)

- [ ] Онлайн-оплата взносов (если есть партнёр или эквайринг).
- [ ] Интеграция с Telegram/WhatsApp (денайджесты, не полная замена чата).
- [ ] Расширенная аналитика для совета (явка, история решений).

---

*Документ можно резать на GitHub Issues / Linear по мере появления репозитория.*
